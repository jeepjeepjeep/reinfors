/target
# reinfors-nn is excluded from the workspace, so it builds its own target/ + standalone lock (artifacts).
/crates/reinfors-nn/target
/crates/reinfors-nn/Cargo.lock
/dist
*.whl
__pycache__/
*.pyc
.venv/
*.so
/runs
uv.lock
